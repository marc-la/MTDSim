"""L2 — GASP (motivation-subgraphed APT Profile).  *Stub.*

Stage L2 of the L0->L4 pipeline: condition the L1 GAP on a motivation specifier
to yield a motivation-subgraph (GASP). Not yet built on this branch — see this
package's ``README.md`` and ``docs/specs/architecture.md`` §(e). Prior art (the
v0.4 terminal-node-ancestor selectors) lives on ``feat/attacker-profiling``; per
the project's zero-trust stance any build here is fresh, not a port.
"""
