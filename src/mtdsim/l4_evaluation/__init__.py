"""L4 — Evaluation.  *Substrate seam — holds no code here.*

Stage L4 of the L0->L4 pipeline: comparative effectiveness measures (internal
MTTC, ASR, attack-path exposure, RoA) over MTDSim runs. The metrics pipeline is
inherited substrate code (``mtdnetwork/``), not part of this package — see this
directory's ``README.md`` and ``docs/specs/architecture.md`` §(g),(i). This
package exists only to mark the pipeline level and point at the seam.
"""
