---
status: durable
created: 2026-05-27
updated: 2026-08-13
---

# Provenance map — load-bearing constants and rules → source → code → disposition

A single cross-link table. For each non-obvious design choice in the
codebase, this names the source (with locator), the file:symbol it lives
at, and the spec disposition. The in-code anchor comments at the listed
file locations point back here and to [`mtdsim_spec.md`](mtdsim_spec.md) /
[`metrics_semantics.md`](metrics_semantics.md).

**Scope.** Entries are restricted to "things a reader would otherwise
wonder where they came from" — i.e. load-bearing values and rules that
have a citeable paper or spec-row source. Obvious or inherited-without-
paper values are not listed.

**Verification.** Every row's source column is taken verbatim from an
existing verified doc in this repo
([`mtdsim_spec.md`](mtdsim_spec.md),
[`../extractions/`](../sources/extractions/),
[`metrics_semantics.md`](metrics_semantics.md),
[`../../baseline/CHANGELOG.md`](../../baseline/CHANGELOG.md)). No source has
been backfilled from outside those.

---

| Concept | Source + locator | Code location (file:symbol) | Disposition |
|---|---|---|---|
| **Network Compromise Ratio (NCR) termination threshold** = 0.8 | Zhang 2023 §5 (NCR terminating condition) | [`mtdnetwork/component/time_network.py`](../../mtdnetwork/component/time_network.py): `TimeNetwork.__init__` default `terminate_compromise_ratio=0.8`; rule applied in `is_compromised` | **faithful** (fixed Phase 2b R1; see mtdsim_spec.md MET-15 / C6) |
| **MTD execution durations** (mean, std) per technique | Zhang 2023 Table 3 (MTD execution time) | [`mtdnetwork/data/constants.py`](../../mtdnetwork/data/constants.py): `MTD_DURATION` dict | **faithful** (re-aligned Phase 2c MTD-14; CompleteTopologyShuffle 120→110, IPShuffle 110→100) |
| **OS-gated exploit success** — an OS-dependent vulnerability cannot be exploited on a host whose OS is not in its OS list; a refused attempt counts as a failed attempt and draws no RNG. Generation: `VULN_PERCENT_CROSS_PLATFORM = 0.5` (cross-platform services), `VULN_PROB_DEPENDS_ON_OS = 0.8`, list = random proper subset of `OS_TYPES` | Brown 2023 §III-B(6) (OS diversity "avoiding any OS-specific exploits") — reclassified 2026-08-27 from D-19's "abandoned alternative" to documented intent; the ×2.5 time term (IS-TIM-06) stays beyond-paper and native-arm-only | [`mtdnetwork/component/services.py`](../../mtdnetwork/component/services.py): `Vulnerability.network` gate; generation :43-47 | **faithful (intent)**; the probabilities and the subset rule are inherited constants documented nowhere — declared. Both arms; goldens re-baselined 2026-08-27 |
| **OS diversity redraws only OS-incompatible services** | Brown §III-B(6); Zhang §4.3.1.4 ("services incompatible with the new OS are also randomly changed") | [`mtdnetwork/mtd/osdiversity.py`](../../mtdnetwork/mtd/osdiversity.py); `service_is_compatible_with_os` in `services.py` | **faithful** (D-18 repaired 2026-08-27, D-05 precedent; was: every service redrawn, guard inert). Compatibility = the (service name, version) pair, ruled the same day: the generator slices versions per OS version, so a bare name under-states incompatibility |
| **MTD execution durations for the Brown-era three** — HostTopologyShuffle (100, 0.5), PortShuffle (70, 0.5), UserShuffle (20, 0.5) | **documented nowhere** (IS-TIM-03): Brown quotes no per-technique duration; Zhang's Table 3 covers only his four. Values are the inherited code's, kept as declared constants on the pool restoration ruling (2026-08-27) | [`mtdnetwork/data/constants.py`](../../mtdnetwork/data/constants.py): `MTD_DURATION` | **declared** (undocumented inheritance; matters only under contention — same-layer suspension — and for the `full` pool) |
| **MTD priority ordering** — CTS 1 > HTS 2 > IP 3 > OSD 4 > Port 5 > SD 6 > User 7 | **documented nowhere** (IS-SCH-06): priority exists as a concept in Zhang §4.3.2–4.3.3, the ordering is never specified | [`mtdnetwork/data/constants.py`](../../mtdnetwork/data/constants.py): `MTD_PRIORITY` | **declared** (inherited code order; read only when two registered mechanisms contend under `simultaneous`) |
| **Mechanism pools** — `lineage` = Zhang's four (default, every recorded experiment, Tay's action space); `full` = Brown's pool restored (the four + HostTopologyShuffle, PortShuffle, UserShuffle); OSDiversityAssignment in no pool | Zhang §4.3.1 (selected four of Brown's seven); Brown §III-B (the pool); ruling 2026-08-27 ([`../handoffs/2026-08-27_mtd_pool_restoration.md`](../handoffs/2026-08-27_mtd_pool_restoration.md)); D-17(c) withdraws DAP | [`mtdnetwork/component/mtd_scheme.py`](../../mtdnetwork/component/mtd_scheme.py): `MTDScheme.MTD_POOLS`, `pool=` kwarg (threaded through `MTDOperation`) | **faithful** — the default pool is untouched so lineage-arm streams stay comparable; the `full` pool is Brown's set with the three latent defects repaired (D-31, D-32/D-26) |
| **MTD trigger interval** = µ + Exp(0.5) per scheme under the default regime — a *shifted* exponential (σ = 0.5, quasi-periodic at the operating means), **not** Exponential(µ); a true Exponential(µ) regime is selectable per run since 2026-08-13 (`--timing-regime exponential`) | Zhang 2023 §4.3.4 (exponential replaces Brown's uniform); Table 3's printed σ = 0.5 vs §4.5's µ-as-mean is Zhang's internal inconsistency — D-08 | [`mtdnetwork/data/constants.py`](../../mtdnetwork/data/constants.py): `MTD_TRIGGER_INTERVAL`; sampled via `time_generator.exponential_variates` (regime seam in [`mtdnetwork/component/time_generator.py`](../../mtdnetwork/component/time_generator.py)) from [`mtdnetwork/operation/mtd_operation.py`](../../mtdnetwork/operation/mtd_operation.py) | **diverged** from Brown's Uniform(1000, 5000) ms (Brown §IV) — superseded by Zhang's exponential family (see mtdsim_spec.md MTD-10 / MTD-11); distribution fork ruled as a declared regime input, default `shifted` — every recorded figure rides it (intent_conformance_audit.md §n D-08, 2026-08-13; corrected here same date — this row previously read "Exponential(µ)") |
| **Host Compromise Ratio (HCR)** = C_t / T_host | Ho 2024 §3.3.2 (#4) | [`mtdnetwork/statistic/evaluation.py`](../../mtdnetwork/statistic/evaluation.py): `evaluation_result_by_compromise_checkpoint`, `host_comp_ratio = self.compromised_num(record=sub_record) / host_num` | **faithful** (fixed Phase 2c C8 / MET-04; HCR ∈ [0, 1] regression-tested) |
| **Internal MTTC** = mean duration over `SCAN_PORT`, `EXPLOIT_VULN`, `BRUTE_FORCE` rows up to compromise checkpoint | Zhang 2023 §3.4; Ho 2024 §3.3.2 (#8) | [`mtdnetwork/statistic/evaluation.py`](../../mtdnetwork/statistic/evaluation.py): `evaluation_result_by_compromise_checkpoint`, `time_to_compromise = attack_duration_series.sum() / attack_action_count` | **faithful (computation)**; magnitude shifted by C7 + ATK-04 (cross-paper numeric comparison invalid — see metrics_semantics.md §d) |
| **Service-compromise threshold** = 7 (against Σ exploited impact in [0, 10]) | README; not in any source paper at this granularity | [`mtdnetwork/data/constants.py`](../../mtdnetwork/data/constants.py): `SERVICE_COMPROMISED_THRESHOLD = 7`; used in [`mtdnetwork/component/services.py`](../../mtdnetwork/component/services.py): `Service.is_exploited` | **faithful** (inherited; threshold and impact range jointly documented, see mtdsim_spec.md NET-14 / C3) |
| **Vuln impact range** = [0, 10] | code (inherited); cf. Brown 2023 Table I [0, 1] | [`mtdnetwork/component/services.py`](../../mtdnetwork/component/services.py): `Vulnerability.__init__`, `self.impact = random.random() * 10` | **diverged** from Brown paper [0, 1]; docs aligned Phase 2c — delta inherited not actioned (would also require recalibrating threshold of 7); see mtdsim_spec.md NET-13 / C3 |
| **Vuln attack complexity range** = [0.4, 1] | Brown 2023 Table I (Brown matches code); cf. Zhang §4.4.3 [0, 1] | [`mtdnetwork/data/constants.py`](../../mtdnetwork/data/constants.py): `VULN_MIN_COMPLEXITY = 0.4`; [`mtdnetwork/component/services.py`](../../mtdnetwork/component/services.py): `Vulnerability.__init__` | **faithful (Brown)**; Zhang paper-side wording differs — see mtdsim_spec.md NET-12 / C2 |
| **Per-host attack-attempt limit (give-up rule)** = 10 | Brown 2023 §III-C(2), Table I (=10); §V-C ("never give up on target node in Scenario 2") | [`mtdnetwork/data/constants.py`](../../mtdnetwork/data/constants.py): `ATTACKER_THRESHOLD = 10`; applied in [`mtdnetwork/operation/attack_operation.py`](../../mtdnetwork/operation/attack_operation.py): `_do_enum_host` (`:268-273`) — `_attack_counter == _attack_threshold` ⇒ push to `stop_attack`, **only when** `network_type == 0` and the host is not the target | **faithful (repaired 2026-07-27)** — was "faithful" on the constant but silently broken in the rule. The guard was inverted against Brown B-ATK-06 — it applied the rule only when `network_type == 0` (the targeted scenario, unreachable here), so no host was ever given up and hosts were re-enumerated up to 50 times against a bound of 10. Polarity restored; attempts are now bounded at 10. See mtdsim_spec.md ATK-07 and baseline/CHANGELOG.md |
| **Global attack-attempt cap** = `5 × total_nodes` | inherited (Brown-era heuristic, not in any paper) | [`mtdnetwork/data/constants.py`](../../mtdnetwork/data/constants.py): `HACKER_ATTACK_ATTEMPT_MULTIPLER = 5`; computed in [`mtdnetwork/component/adversary.py`](../../mtdnetwork/component/adversary.py): `Adversary.__init__._max_attack_attempts` | **diverged (inert) — left inert deliberately** (2026-07-27). `_curr_attempts` is incremented in `_do_exploit_vuln` (`attack_operation.py:386`); the single enforcement site (`attack_operation.py:276-278`, in `_do_enum_host`) is commented out. Locators refreshed — the previous `:220` predated the carve. The cap is genuinely overrun (481 attempts against 250 in the seeded golden), so restoring the guard would truncate every run and re-baseline the goldens. See mtdsim_spec.md ATK-08 |
| **Exploit-time model** — deterministic `ATTACK_DURATION['EXPLOIT_VULN'] * (1 - complexity)` (×2.5 on OS-mismatch; ÷2 on already-exploited *instance*) | (code reality differs from paper) | [`mtdnetwork/component/services.py`](../../mtdnetwork/component/services.py): `Vulnerability.exploit_time` | **diverged (C7 / ATK-03)** — Zhang 2023 §4.4.3 Eq 1-2 specifies an exponential `T_Aexploit` parameterised by `V_exploited`/`V_unexploited`/`ACv`; deterministic form retained as inherited reality, shifts absolute MTTC magnitude (see metrics_semantics.md §c). **Native arm only since S3-R (2026-07-28):** the movement arm supplies its own action durations, so `exploit_time` — and with it the complexity scaling and the OS-mismatch multiplier — is never evaluated there. Unchanged and live on the native FSM arm. |
| **Re-exploit time discount — per-instance (active)** | Brown 2021 (commit `a16db997`, 2021-09-04); not in any source paper | [`mtdnetwork/component/services.py`](../../mtdnetwork/component/services.py): `Vulnerability.exploit_time`, `if self.exploited: return exp_time / 2` (lines 90-91) | **active inherited mechanism** — halves the exploit-time cost when re-attempting the *same `Vulnerability` instance*. Brown-era, pre-Zhang. Fires in 7–42 % of `exploit_time` calls across the 9 goldens (pinned by `tests/test_atk04_reexploit_discount.py`). Kept deliberately (Unit C disposition). See metrics_semantics.md §c / mtdsim_spec.md ATK-04. **Native arm only since S3-R (2026-07-28):** the discount is expressed purely through `exploit_time`, which the movement arm no longer evaluates, so the mechanism is inert there. Worth noting because it was the model's only per-instance memory of a prior attempt — a small loss of adaptivity on the profiled arm, and the axis-7 learning row in `apt_model_criterion.md` was NOT ADDRESSED before or after, so no badge moves. |
| **Attacker learning — per-type (missing)** | Zhang 2023 §4.4.3 | [`mtdnetwork/component/services.py`](../../mtdnetwork/component/services.py): `Vulnerability.exploit_time` — trace at the commented-out `exploit_attempt + 1` line (lines 97-98) | **diverged (ATK-04, missing)** — Zhang's per-vuln-TYPE halving (the cross-instance learning rule) is unimplemented; only the commented-out trace remains. Distinct mechanism from the per-instance discount above. See metrics_semantics.md §c / mtdsim_spec.md ATK-04. |
| **Attack-action enums** (metrics-relevant) = `SCAN_PORT`, `EXPLOIT_VULN`, `BRUTE_FORCE` | Ho 2024 §3.3.2 (MTTC + ASR + Attack Stage) | Used in [`mtdnetwork/statistic/evaluation.py`](../../mtdnetwork/statistic/evaluation.py): `evaluation_result_by_compromise_checkpoint` `name.isin(['SCAN_PORT', 'EXPLOIT_VULN', 'BRUTE_FORCE'])` | **faithful**; see mtdsim_spec.md ATK-13 |
| **Three-phase action timing** (Phase 1 SCAN_PORT + cred stuff; Phase 2 EXPLOIT_VULN; Phase 3 BRUTE_FORCE) | Zhang 2023 §4.4.2, §4.4.3 | [`mtdnetwork/data/constants.py`](../../mtdnetwork/data/constants.py): `ATTACK_DURATION` dict; phase rotation in [`mtdnetwork/operation/attack_operation.py`](../../mtdnetwork/operation/attack_operation.py) | **faithful (timing constants)**; Phase-2 distribution diverged per C7 above |
| **MTD interrupt + confusion penalty** = `ATTACK_DURATION['PENALTY']` = 20 | Brown 2023 §V-A; Zhang 2023 §4.4.3 | [`mtdnetwork/data/constants.py`](../../mtdnetwork/data/constants.py): `ATTACK_DURATION['PENALTY'] = 20`; applied in [`mtdnetwork/operation/attack_operation.py`](../../mtdnetwork/operation/attack_operation.py): `apply_mtd_interrupt_cost`, consumed by the native FSM via `_handle_interrupt` and by the movement driver via `_read_interrupt` | **faithful**; see mtdsim_spec.md ATK-05. **S3 ruled it stays substrate-side (2026-07-28):** an earlier draft would have re-homed it onto the movement layer as a place in the net; that was reversed because how a defender *thwarts* an attacker is simulator-specific, and the movement layer is meant to be portable across simulators through the controller ([`pipeline/ogasp/stochastic_timing_design.md`](pipeline/ogasp/stochastic_timing_design.md) §4). Both arms already consume the one call, so S3's requirement — the same stochastic draw about the same base duration on both arms — was already met and needed no build. Single-charge-per-interrupt is guarded by [`tests/test_action_layer_dispositions.py`](../../tests/test_action_layer_dispositions.py) `test_the_confusion_penalty_is_charged_once_per_interrupt`, which permits exactly one in-flight charge when the horizon cuts a run mid-penalty and no other excess. |
| **Cross-platform vuln chance** = 0.5 | Brown 2023 Table I; restated Zhang §4.2.3 ("half") | [`mtdnetwork/data/constants.py`](../../mtdnetwork/data/constants.py): `VULN_PERCENT_CROSS_PLATFORM = 0.5` | **faithful**; see mtdsim_spec.md NET-11 |
| **Patched-version mean / range** = 10 / 9 | Brown 2023 §III-A | [`mtdnetwork/data/constants.py`](../../mtdnetwork/data/constants.py): `VULN_PATCH_MEAN = 10`, `VULN_PATCH_RANGE = 9` | **faithful**; see mtdsim_spec.md NET-10 |
| **Services per host** ∈ [3, 11] | Brown 2023 Table I | [`mtdnetwork/data/constants.py`](../../mtdnetwork/data/constants.py): `HOST_SERVICES_MIN = 3`, `HOST_SERVICES_MAX = 11` | **faithful**; see mtdsim_spec.md NET-06 |
| **Users per host** = 5; **password-reuse chance** = 0.05 | Brown 2023 §III-B(3); Zhang 2023 §4.2.2 | [`mtdnetwork/data/constants.py`](../../mtdnetwork/data/constants.py): `USER_TOTAL_FOR_EACH_HOST = 5`, `USER_PROB_TO_REUSE_PASS = 0.05` | **faithful**; see mtdsim_spec.md NET-07 |
| **L3 transition-weight regime** — tactic-pair transition weight = out-edge-normalised **flow proportion** (proportion of attack flows leaving the source tactic along that pair); technique-level `observation_count` stays recorded, un-normalised | Supervisor decision D3 (Jin Hong working session, July 2026; register at [`../notes/2026-07-03_supervisor_meeting_l3_decisions.md`](pipeline/ogasp/supervisor_decision_register.md)); operative disposition in [`metrics_semantics.md`](metrics_semantics.md) §(f) | [`src/mtdsim/l3_simulation/petri/`](../../src/mtdsim/l3_simulation/petri/) (build); shipped as the weight layer of [`data/ogasp/*_structural.json`](../../data/ogasp/README.md) (two corpus variants: `operator_dedup` n = 29 primary, `raw` n = 38); gate in [`tests/l3_simulation/test_weights.py`](../../tests/l3_simulation/test_weights.py) | **dispositioned, built** — the closed-world assumption and survivorship framing of metrics_semantics.md §(f) are mandatory wherever a weight is cited |
| **Synthetic overlay** — declared pre-intrusion structure (bidirectional connective tissue): forward chain `reconnaissance → resource-development → initial-access` (declared routing share 1.0 each, sole out of an island place) + backward regression bridge `initial-access → reconnaissance` (declared share 0.1 of initial-access's composed out-mass), added where the observed corpus leaves the pre-intrusion band detached (`objective_exfiltration_impact`, `objective_none_c2`) | Supervisor decision M6 ([`pipeline/ogasp/supervisor_decision_register.md`](pipeline/ogasp/supervisor_decision_register.md) §M6); authoritative record [`pipeline/ogasp/synthetic_overlay.md`](pipeline/ogasp/synthetic_overlay.md) | [`src/mtdsim/l3_simulation/petri/synthetic_overlay.py`](../../src/mtdsim/l3_simulation/petri/synthetic_overlay.py): `FORWARD_SHARE`/`BACKWARD_SHARE`, `curate_synthetic_overlay`; artefact [`data/ogasp/petri/synthetic_overlay.json`](../../data/ogasp/petri/synthetic_overlay.json); gate in [`tests/l3_simulation/test_petri.py`](../../tests/l3_simulation/test_petri.py) §7 | **editorial/synthetic (declared)** — no flow backing, flagged `synthetic`, kept outside the observed nets as a composed overlay (observed `*_structural.json` byte-identical). The forward chain perturbs no observed distribution (sole out of island places). The **one declared exception**: the backward bridge leaves `initial-access` (which has observed out-edges), so it carries a declared share and the observed edges keep their relative proportions across the remainder — the only place the overlay renormalises a D3 distribution, confined to that edge and flagged for review. `resource-development` is now a bridged live pass-through (reversing the earlier recon-only resolution, Marc's direction) |
| **L3 outcome (policy) overlay** — the M2 policy layer: a **ground-up conditional-likelihood weighting over the whole directed tactic-pair set**. For each pair `a→b` and verdict `v∈{success,failure}`, a declared value in [0,1] = "given the action at `a` came back `v`, the likelihood of `b` next" (0 = won't happen, 1 = most likely next). Composed multiplicatively with the D3 base weights + the substrate's binary verdict at runtime, renormalised per source place. Whole-space coverage (all sources incl resource-development, forward + backward); runtime consumption tracks verdict availability (the action set is extensible). Values reasoned per-pair from kill-chain band + capability enablement (`enables`) + foothold dependency | Supervisor decision M2/M3 ([`pipeline/ogasp/supervisor_decision_register.md`](pipeline/ogasp/supervisor_decision_register.md) §M2/§M3); design in [`pipeline/ogasp/success_failure_overlay_design.md`](pipeline/ogasp/success_failure_overlay_design.md); per-verb verdict oracle [`pipeline/ogasp/controller.md`](pipeline/ogasp/controller.md) §4; success-pattern grounding [`../sources/tactic_profiles/step_c/`](../sources/tactic_profiles/step_c/) (Sophos/DFIR AARs) | canonical rule-set [`data/ogasp/controller/outcome_rules.json`](../../data/ogasp/controller/outcome_rules.json), compiled to the 210-pair views in the **versioned registry** [`data/ogasp/controller/overlays/`](../../data/ogasp/controller/overlays/) (`v1_band_relationship` = experiment 1's, frozen; `v2_lifecycle_distance` = the S1 fold-in; default stays at experiment 1's) by the generator [`src/mtdsim/l3_simulation/controller/rules.py`](../../src/mtdsim/l3_simulation/controller/rules.py), whose `--check` re-derives every committed cell (0/420 per version); corpus-scoped R2 view [`data/ogasp/petri/outcome_overlay.json`](../../data/ogasp/petri/outcome_overlay.json); composition at [`src/mtdsim/l3_simulation/controller/outcome.py`](../../src/mtdsim/l3_simulation/controller/outcome.py) | **editorial/synthetic (declared)** — a declared **policy** layer, not flow-derived and not reverse-engineered from the nets; conditions the grounded D3 base weights, never re-derives them; envelope-not-actor. The **evidence-tier asymmetry is load-bearing**: `success` grounded in MITRE tactic semantics + AAR-attested enablement, `failure` is declared judgement (no report documents failure behaviour). **R2 finalised 2026-07-23** (four adversarial rounds, converged, certified 82%) and consumed by experiment 1; **reframed by S1 (2026-07-21) as an initial trial of static weights**. **S1 executed 2026-07-28** ([`pipeline/ogasp/weight_sensitivity_study.md`](pipeline/ogasp/weight_sensitivity_study.md)): the declared lifecycle-distance kernel now multiplies each rule value and the `relationship` term is re-sourced from the consensus stages (40 of 210 pairs re-classed; **no R2 rule value changed**), and the magnitudes carry a **mixed sweep verdict** — ASR-zero and MTD-invariance hold across the declared bands, the intermediate profile's failure-mode classification and any profile ordering move, and the floor `z` is behaviourally inert because no profile net carries a three-stage transition. Dynamic state-conditioned weights remain deferred. |
| **Lifecycle consensus ordering + tactic-to-tactic distance model** (S1, literature half) — five published APT-lifecycle models (Hutchins CKC primary; Alshamrani five-phase; Mandiant APT1 lifecycle — primary verified 2026-07-27, [`mandiant2013`](../sources/extractions/mandiant2013.md); Ussath three-stage via Alshamrani; Che Mat's derivative-family skeleton) overlaid onto the 15 tactics; consensus = four super-stages `prep ≺ intrusion ≺ post-intrusion(weakly ordered) ≺ objective`; distance = per-direction geometric decay `γ^(Δ−1)` forward / `δ^(\|Δ\|−1)` backward with zero floor `z` over the signed stage offset | Supervisor ruling S1 ([`pipeline/ogasp/supervisor_decision_register.md`](pipeline/ogasp/supervisor_decision_register.md) §S1, method: overlay → consensus → fold); M3 (ATT&CK imposes no ordering — the import is an assumption); extractions [`hutchins2011`](../sources/extractions/hutchins2011.md) §3.2, [`alshamrani2019`](../sources/extractions/alshamrani2019.md) §II-C, [`chemat2024`](../sources/extractions/chemat2024.md) Table 4; authoritative record [`pipeline/ogasp/lifecycle_consensus.md`](pipeline/ogasp/lifecycle_consensus.md) | artefact [`data/ogasp/controller/lifecycle_consensus.json`](../../data/ogasp/controller/lifecycle_consensus.json) (stages, seats + evidence status, kernel, parameters, sweep bands); **no code consumes it yet** — the fold-in is the sensitivity-study handoff | **mixed, separable by construction**: the four-stage spine and most seats are *sourced* (locator-cited); six seats are *rule-resolved* (declared rules R-1/R-2 over sourced disagreements — C2, persistence, priv-esc, cred-access, discovery, lateral-movement); the kernel magnitudes `γ=0.25, δ=0.5, z=0.1` and the defense-impairment seat are *declared* (tier `attested-pattern/declared-magnitude` / `declared-judgement`), and only the declared magnitudes are swept. Verdict on the five-band prior: bands 0/1/objective survive as sourced, the consolidate≺expand split and C2's band-4 seat do not. Not fitted: no value chosen by observing net traversal (CTI-independence boundary) |
| **L3 per-tactic state-duration regime** — tiered sourcing: (1) substrate verbs (`ATTACK_DURATION`) where a tactic maps onto them, (2) literature timing values where they exist, (3) reasonable **justified estimate** otherwise (e.g. stealth); every value carries provenance + a sweep range | Supervisor decision D4 (same session; no ready-made MITRE-tactic→time resource exists — defined here, with justifications); method + tier bar in [`../notes/2026-07-04_operational_validation_the_bar.md`](../notes/ch4_methods/operational_validation.md); evidence layer in [`../tactic_profiles/`](../notes/ch4_methods/tactic_profiles/) §5 | [`data/ogasp/tactic_durations.json`](../../data/ogasp/tactic_durations.json) (v0 uncalibrated); the draw at [`src/mtdsim/l3_simulation/movement/timing.py`](../../src/mtdsim/l3_simulation/movement/timing.py), consumed at the single `_walk` timing seam in [`movement/attacker.py`](../../src/mtdsim/l3_simulation/movement/attacker.py); gates in [`tests/l3_simulation/test_durations.py`](../../tests/l3_simulation/test_durations.py) (key set = place-union; corpus-frequency guard; anchor arithmetic; magnitude sanity; the two-consumer semantics guard) and [`tests/l3_simulation/test_movement_timing.py`](../../tests/l3_simulation/test_movement_timing.py) (the distribution, its determinism and stream isolation, and the regime inside the walk) | **dispositioned, built — stochastic regime landed 2026-07-28 (S3), scope widened the same day (S3-R).** Under **S3-R the movement layer supplies every unit of the attacker's time**: a tactic's draw *is* the dispatched action's duration, passed to `step(verb, duration=...)`, and the substrate's own `ATTACK_DURATION` / `exploit_time` are **not consumed on that arm** (they remain the native FSM's pricing, untouched). Because the mapping is many-to-one, the same verb costs different amounts under different tactics — the price belongs to the modelled behaviour, not to the verb. Every place visit costs its tactic's time, including an attempt blocked by an unmet precondition (effort spent, nothing achieved) and a dwell-only place. The values are unchanged; what changed is what reads them. **Two consumers, two readings, both declared in the artefact's own metadata:** the L3 **movement layer** reads each `duration_s` as the **mean of a per-tactic exponential firing time** (GSPN timed-transition semantics — the D10 deferral is lifted), while the standalone **timeline runner** (D1 analytical track) still reads it as a deterministic **point** dwell under its own SIM-05 discipline. A `duration_s` of 0 is a GSPN *immediate* transition, never a degenerate `Exponential(mean 0)`. The tier badges, group anchors and sweep bands carry over **unaltered** — a declared value simply became a distribution's centre — and the supervisor's constraint stands: the arbitrariness is inherent, so the *justification* is the deliverable (defence, with its literature grounding and its acknowledged counter-evidence, in [`pipeline/ogasp/stochastic_timing_design.md`](pipeline/ogasp/stochastic_timing_design.md) §3.2). **Two things S3 did *not* do:** the per-tactic values were not recalibrated (that stays post-MVP work under the existing validity framework, per the 2026-07-10 written feedback — register R1/R4), and the confusion penalty was **not** re-homed into the net (see the row above: it stays substrate-side on portability grounds, [`stochastic_timing_design.md`](pipeline/ogasp/stochastic_timing_design.md) §4). **Per-tactic rows below await Marc's approval.** |
| **L3 within-run learning capability** (criterion axis 7) — a place-keyed Laplace belief `Q(b) = (s_b+α)/(s_b+f_b+α+β)` over this run's success/failure verdicts, entering routing as a third multiplicative factor `Q(b)^κ` and perishing on every MTD interrupt by `s_b, f_b ← (1−ρ)·s_b, (1−ρ)·f_b` | Cho 2020 §V-D (attacker learning named as the sharpest under-developed dimension) and Jalowski 2026 §4.3 ("learns mutation patterns over time") supply the *behaviour*; Alshamrani 2019 §IV-C-2-B (rearrangement "renders the exploratory knowledge of the attacker useless") supplies the mechanism the forgetting rule models. Authoritative record [`pipeline/ogasp/learning_capability.md`](pipeline/ogasp/learning_capability.md); precedent [`declared_value_provenance.md`](declared_value_provenance.md) §6.2 | rules [`data/ogasp/movement/learning_rules.json`](../../data/ogasp/movement/learning_rules.json); compiled worked view [`data/ogasp/movement/learning_factors.json`](../../data/ogasp/movement/learning_factors.json) by the generator [`src/mtdsim/l3_simulation/movement/learning.py`](../../src/mtdsim/l3_simulation/movement/learning.py) (`--check` re-derives 0/186 cells); registered as a modulator on the attacker-state seam [`movement/state.py`](../../src/mtdsim/l3_simulation/movement/state.py); gates in [`tests/l3_simulation/test_movement_learning.py`](../../tests/l3_simulation/test_movement_learning.py) | **editorial/declared — built 2026-07-29, swept, badge decided against a pre-registered criterion.** Two declared parameters: `κ = 1` (tier `attested-pattern/declared-magnitude` — the behaviour is attested, the magnitude is not) and `ρ = 0.5` (tier `declared-judgement` — the literature's plain reading sits at the ρ=1 pole, so the midpoint is a modelling hedge and is labelled one). The estimator's prior `α = β = 1` is deliberately **not swept**: any asymmetric prior asserts a belief about how often tactics pay, settable only by looking at the layer being conditioned. **CTI-independence holds by construction** — the authored artefact is the learning *rule*, the corpus supplies the prior, and `κ = 0` recovers that prior bit for bit (asserted field-for-field across 5 profiles × 5 seeds × 2 MTD conditions × 2 mappings). Within-run only; no memory crosses a run. Distinct from the ATK-04 row above, which is a substrate pricing discount, not a decision capability, and was left untouched. |

---

## L3 state-duration catalogue — per-tactic rows (**pending Marc's approval**)

The row-level detail behind the D4 regime row above. Values are the
`v0-uncalibrated` catalogue ([`data/ogasp/tactic_durations.json`](../../data/ogasp/tactic_durations.json));
each traces to its profile's §5 block (the single source of truth) and must
stay consistent with dissertation.tex §3.1 Tables 3.1–3.2. Units: simulated
seconds (the `env.timeout` clock). `duration_s` = multiplier × group anchor.
Anchors: **scan-shaped** 35 s (`ATTACK_DURATION` `SCAN_HOST` 5 + `SCAN_NEIGHBOR` 5
+ `SCAN_PORT` 25 — one enumeration pass; Tier 1, not tuned); **exploit-shaped**
4.5 s (median `exploit_time` = `EXPLOIT_VULN` 15 × (1 − complexity), complexity
~ U[0.4, 1]; Tier 1, not tuned); **stealth-low-and-slow** 45 s (declared v0:
10 × exploit median — an order of magnitude above the priced verbs, below the
200 s MTD trigger-interval mean; Tier 3); **objective-execution** 36 s (declared
v0: 8 × exploit median; Tier 2, calibratable against Bromiley/Sophos/ransomware-IR
milestones); **prep-off-network** 0 s. The §3 reset verdicts are a separate
declared parameter for the L3b binding — deliberately not in this catalogue.

| Tactic | Group | × | `duration_s` | Tier | Sweep (× anchor) | Source |
|---|---|--:|--:|:--:|---|---|
| `reconnaissance` | scan-shaped | 1.0 | 35.0 | 1 | 0.5–2 | `ATTACK_DURATION` scan verbs; [01 §5](../notes/ch4_methods/tactic_profiles/01_reconnaissance.md) |
| `resource-development` | prep-off-network | 0 | 0.0 | 3 | degenerate (0) | declared off-clock; [02 §5](../notes/ch4_methods/tactic_profiles/02_resource-development.md) |
| `initial-access` | exploit-shaped | 1.0 | 4.5 | 1 | 0.5–2 | `exploit_time`; [03 §5](../notes/ch4_methods/tactic_profiles/03_initial-access.md) |
| `execution` | stealth-low-and-slow | 0.5 | 22.5 | 3 | 0.1–2 | declared (group unsettled); [04 §5](../notes/ch4_methods/tactic_profiles/04_execution.md) |
| `persistence` | stealth-low-and-slow | 1.0 | 45.0 | 3 | 0.25–4 | declared; [05 §5](../notes/ch4_methods/tactic_profiles/05_persistence.md) |
| `privilege-escalation` | exploit-shaped | 1.0 | 4.5 | 1 | 0.5–2 | `exploit_time`; [06 §5](../notes/ch4_methods/tactic_profiles/06_privilege-escalation.md) |
| `stealth` | stealth-low-and-slow | 1.0 | 45.0 | 3 | 0.25–4 | declared (group reference); [07 §5](../notes/ch4_methods/tactic_profiles/07_stealth.md) |
| `defense-impairment` | stealth-low-and-slow | 0.5 | 22.5 | 3 | 0.1–4 (widest) | declared (group unresolved); [08 §5](../notes/ch4_methods/tactic_profiles/08_defense-impairment.md) |
| `credential-access` | exploit-shaped | 1.0 | 4.5 | 1 | 0.5–2 | `exploit_time` (dumping path); [09 §5](../notes/ch4_methods/tactic_profiles/09_credential-access.md) |
| `discovery` | scan-shaped | 1.0 | 35.0 | 1 | 0.5–2 | `ATTACK_DURATION` scan verbs; [10 §5](../notes/ch4_methods/tactic_profiles/10_discovery.md) |
| `lateral-movement` | exploit-shaped | 1.0 | 4.5 | 1 | 0.25–4 (wide) | `exploit_time`; [11 §5](../notes/ch4_methods/tactic_profiles/11_lateral-movement.md) |
| `collection` | objective-execution | 1.0 | 36.0 | 2 | 0.5–2 | [`collection_exfil_timing`](../sources/extractions/collection_exfil_timing.md); [12 §5](../notes/ch4_methods/tactic_profiles/12_collection.md) |
| `command-and-control` | stealth-low-and-slow | 1.0 | 45.0 | 3 | 0.25–4 | declared (un-priceable by CVE data); [13 §5](../notes/ch4_methods/tactic_profiles/13_command-and-control.md) |
| `exfiltration` | objective-execution | 1.0 | 36.0 | 2 | 0.25–4 | [`breach_reports_macro_timing`](../sources/extractions/breach_reports_macro_timing.md) (held-out milestone); [14 §5](../notes/ch4_methods/tactic_profiles/14_exfiltration.md) |
| `impact` | objective-execution | 1.0 | 36.0 | 2 | 0.1–5 (widest) | [`ransomware_timing`](../sources/extractions/ransomware_timing.md); [15 §5](../notes/ch4_methods/tactic_profiles/15_impact.md) |

---

## Flags — claims worth re-dispositioning

These are observations made during the provenance pass that don't have a
clean disposition in the verified docs and need Marc's call.

- ~~**ATK-04 active code at `services.py:84-85`**~~ — **resolved in Unit C**.
  Both mechanisms now have rows above (per-instance Brown-era discount =
  active and kept; per-type Zhang learning = unimplemented), and the
  in-code anchor comment at `services.py:80-99` and the doc sections at
  [`metrics_semantics.md`](metrics_semantics.md) §c and
  [`mtdsim_spec.md`](mtdsim_spec.md) ATK-04 have been disambiguated.
  Discount-fire counts are pinned across the 9 goldens by
  [`tests/test_atk04_reexploit_discount.py`](../../tests/test_atk04_reexploit_discount.py).

- **Out of this pass's scope (not actioned, just noted)** — every other
  `divergent` / `missing` row in [`mtdsim_spec.md`](mtdsim_spec.md) is
  already named in the spec's "Biggest faithfulness risks" §; this
  document does not duplicate them. The two C8 / MET-04 and MTD-14 fixes
  are reflected here as `faithful` because they are post-2c reality.
