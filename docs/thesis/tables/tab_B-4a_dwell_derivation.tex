% GENERATED by tools/dwell_catalogue_tables.py from
%   data/ogasp/tactic_durations.json (v0-uncalibrated); tactic axis and
%   display names from the pinned ATT&CK bundle (v19.1).
% Do not hand-edit; regenerate. Requires booktabs (already in the preamble).

\begin{table}[htbp]
\centering
\scriptsize
\setlength{\tabcolsep}{4pt}
\caption[Derivation of the declared dwell times]{How each declared dwell was arrived at. \emph{Family} is the anchor the value takes its shape from; the per-family multipliers that separate tactics sharing one are in Table~\ref{tab:dwell-catalogue}, which is why two tactics dispatching the same action can hold different dwells. \emph{Sweep band} is the declared band the value may take, in units of its family anchor --- the band is a \emph{parameter} declared here, while what happened when the anchors were moved across their bands is reported in Appendix~\ref{app:sensitivity}. The catalogue that carries these values is the chapter's Table~\ref{tab:dwell-catalogue}.}
\label{tab:dwell-derivation}
\begin{tabular}{@{}l l r c p{0.40\textwidth}@{}}
\toprule
Tactic & Family & Value (s) & Sweep band & Why this value \\
\midrule
Reconnaissance & Scan-shaped & 35.0 & [0.5, 2] & Priced by the simulator as one enumeration pass; the band covers the slower staged tempo it does not meter. \\
Resource development & Off-network prep & 0.0 & --- & Preparation happens off-network, with nothing in-network to meter, so it precedes the simulator's clock. \\
Initial access & Exploit-shaped & 4.5 & [0.5, 2] & Exploiting a public-facing service is what the simulator's complexity-scaled exploit time already prices. \\
Execution & Low-and-slow & 22.5 & [0.1, 2] & A fast cross-cutting act in a low-and-slow wrapper: the pacing is the wrapper's, so the estimate leans fast. \\
Persistence & Low-and-slow & 45.0 & [0.25, 4] & The durability layer built to outlast reboots and reimaging; no simulator verb prices holding a foothold. \\
Privilege escalation & Exploit-shaped & 4.5 & [0.5, 2] & Canonically vulnerability exploitation, which the simulator prices; the band spans direct exploit to token reuse. \\
Stealth & Low-and-slow & 45.0 & [0.25, 4] & The group's reference by construction: a pace imposed on every other action, not a discrete dwell. \\
Defense impairment & Low-and-slow & 22.5 & [0.1, 4] & Widest uncertainty here: evasion is preferred, and disabling defences is a punctuated act before a noisy payload. \\
Credential access & Exploit-shaped & 4.5 & [0.5, 2] & Dumping and cracking are on-host work the simulator prices, with fast ticket theft as the within-group skew. \\
Discovery & Scan-shaped & 35.0 & [0.5, 2] & Reconnaissance's interior twin: the same enumeration run from inside a foothold, so it inherits the scan anchor. \\
Lateral movement & Exploit-shaped & 4.5 & [0.25, 4] & Usually a remote login the simulator prices, but a span from fast worm to slow manual pivot earns a wider band. \\
Collection & Objective execution & 36.0 & [0.5, 2] & The reference act of gathering before exfiltration; a throughput floor keeps the band at fast grab to slow harvest. \\
Command and control & Low-and-slow & 45.0 & [0.25, 4] & A low-signal channel beaconing on a cadence, not a discrete act; its interval is what an MTD move competes against. \\
Exfiltration & Objective execution & 36.0 & [0.25, 4] & The terminal theft act; the band spans a fast bulk burst to a slow fragmented trickle above a throughput floor. \\
Impact & Objective execution & 36.0 & [0.1, 5] & The terminal payload: a fast decisive burst for ransomware, never reached for espionage --- hence the widest band. \\
\bottomrule
\addlinespace[2pt]
\multicolumn{5}{@{}p{0.96\textwidth}@{}}{\scriptsize Values, bands and rationales are emitted from the declared catalogue (\texttt{data/ogasp/tactic\_durations.json}, v0-uncalibrated); tactic names follow ATT\&CK~v19.1. A degenerate band (resource development) is shown as a dash: the tactic is off-clock, so there is nothing to sweep.}\\
\end{tabular}
\end{table}
